node_modules
dist
*.log
.DS_Store
config.bat

# Secrets — never commit real env files (only *example templates are allowed below)
.env
.env.*
.env.local
.env.*.local
.env.production
.env.development
.env.test
!.env.example
!.env.production.example

# TypeScript incremental build cache
tsconfig.build.tsbuildinfo
temp_auto_push.bat
temp_interactive_push.bat

# Local uploaded bid attachments (UPLOAD_ROOT when ./uploads)
uploads/

# Playwright persistent profiles (Trimble / StructShare login cookies).
# These contain session tokens — never commit.
.playwright-profile-*
