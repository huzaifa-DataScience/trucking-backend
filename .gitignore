node_modules
dist
*.log
.DS_Store
config.bat

# Local secrets — never commit
.env
.env.*
!.env.example

# TypeScript incremental build cache
tsconfig.build.tsbuildinfo
temp_auto_push.bat
temp_interactive_push.bat

# Playwright persistent profiles (Trimble / StructShare login cookies).
# These contain session tokens — never commit.
.playwright-profile-*
